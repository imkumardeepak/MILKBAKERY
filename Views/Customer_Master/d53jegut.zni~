@model IEnumerable<Milk_Bakery.Models.Customer_Master>

@{
    ViewData["Title"] = "Customer Master";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container mx-auto px-4 py-6">
    <div class="bg-white rounded-xl shadow-md overflow-hidden">
        <!-- Header Section -->
        <div class="bg-gradient-to-r from-blue-600 to-blue-800 px-6 py-4 flex flex-col sm:flex-row items-center justify-between">
            <div class="mb-4 sm:mb-0">
                <h1 class="text-2xl font-bold text-white">Customer Management</h1>
                <p class="text-blue-100 text-sm mt-1">View and manage all customer records</p>
            </div>
            <a asp-action="AddOrEdit" class="flex items-center justify-center gap-2 px-4 py-2 bg-white text-blue-700 rounded-lg shadow hover:bg-blue-50 transition-colors duration-200 font-medium">
                <i class="fas fa-plus"></i> Add Customer
            </a>
        </div>

        <!-- Table Section -->
        <div class="p-4">
            <!-- Search and Filter -->
            <div class="mb-4 flex flex-col md:flex-row gap-4">
                <div class="relative flex-grow">
                    <input type="text" placeholder="Search customers..." class="w-full pl-10 pr-4 py-2 border rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500 outline-none transition-all">
                    <i class="fas fa-search absolute left-3 top-3 text-gray-400"></i>
                </div>
                <div class="flex gap-2">
                    <select class="border rounded-lg px-3 py-2 focus:ring-2 focus:ring-blue-500 outline-none">
                        <option>Filter by Division</option>
                        <!-- Add options here -->
                    </select>
                    <select class="border rounded-lg px-3 py-2 focus:ring-2 focus:ring-blue-500 outline-none">
                        <option>Filter by State</option>
                        <!-- Add options here -->
                    </select>
                </div>
            </div>

            <!-- Table Container -->
            <div class="border rounded-lg overflow-hidden">
                <div class="overflow-x-auto">
                    <table class="min-w-full divide-y divide-gray-200">
                        <thead class="bg-gray-50">
                            <tr>
                                <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-700 uppercase tracking-wider">
                                    @Html.DisplayNameFor(model => model.Name)
                                </th>
                                <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-700 uppercase tracking-wider">
                                    @Html.DisplayNameFor(model => model.shortname)
                                </th>
                                <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-700 uppercase tracking-wider">
                                    @Html.DisplayNameFor(model => model.Division)
                                </th>
                                <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-700 uppercase tracking-wider">
                                    @Html.DisplayNameFor(model => model.phoneno)
                                </th>
                                <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-700 uppercase tracking-wider">
                                    @Html.DisplayNameFor(model => model.city)
                                </th>
                                <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-700 uppercase tracking-wider">
                                    Actions
                                </th>
                            </tr>
                        </thead>
                        <tbody class="bg-white divide-y divide-gray-200">
                            @foreach (var item in Model)
                            {
                                <tr class="hover:bg-gray-50 transition-colors duration-150">
                                    <td class="px-6 py-4 whitespace-nowrap">
                                        <div class="font-medium text-gray-900">@Html.DisplayFor(modelItem => item.Name)</div>
                                    </td>
                                    <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-600">
                                        @Html.DisplayFor(modelItem => item.shortname)
                                    </td>
                                    <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-600">
                                        @Html.DisplayFor(modelItem => item.Division)
                                    </td>
                                    <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-600">
                                        <a href="tel:@item.phoneno" class="text-blue-600 hover:text-blue-800">@Html.DisplayFor(modelItem => item.phoneno)</a>
                                    </td>
                                    <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-600">
                                        @Html.DisplayFor(modelItem => item.city), @Html.DisplayFor(modelItem => item.state)
                                        <div class="text-xs text-gray-400">@Html.DisplayFor(modelItem => item.PostalCode)</div>
                                    </td>
                                    <td class="px-6 py-4 whitespace-nowrap text-sm font-medium">
                                        <div class="flex space-x-2">
                                            <a asp-action="AddOrEdit" asp-route-id="@item.Id" class="text-blue-600 hover:text-blue-900 p-2 rounded-full hover:bg-blue-50" title="Edit">
                                                <i class="fas fa-pencil-alt"></i>
                                            </a>
                                            <a asp-action="Delete" asp-route-id="@item.Id" onclick="return confirm('Are you sure you want to delete this customer?');" class="text-red-600 hover:text-red-900 p-2 rounded-full hover:bg-red-50" title="Delete">
                                                <i class="fas fa-trash-alt"></i>
                                            </a>
                                            <a href="#" class="text-green-600 hover:text-green-900 p-2 rounded-full hover:bg-green-50" title="View Details">
                                                <i class="fas fa-eye"></i>
                                            </a>
                                        </div>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>

            <!-- Pagination -->
            <div class="mt-4 flex items-center justify-between">
                <div class="text-sm text-gray-600">
                    Showing <span class="font-medium">1</span> to <span class="font-medium">10</span> of <span class="font-medium">@Model.Count()</span> customers
                </div>
                <div class="flex space-x-2">
                    <button class="px-3 py-1 border rounded-md bg-white text-gray-700 hover:bg-gray-50">
                        Previous
                    </button>
                    <button class="px-3 py-1 border rounded-md bg-blue-600 text-white">
                        1
                    </button>
                    <button class="px-3 py-1 border rounded-md bg-white text-gray-700 hover:bg-gray-50">
                        2
                    </button>
                    <button class="px-3 py-1 border rounded-md bg-white text-gray-700 hover:bg-gray-50">
                        Next
                    </button>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        function confirmDelete(e) {
            e.preventDefault();
            if (confirm('Are you sure you want to delete this customer?')) {
                window.location.href = e.target.href;
            }
        }
    </script>
}