@model Milk_Bakery.ViewModels.GatePassViewModel

@{
    ViewData["Title"] = "Print Gate Pass";
    Layout = null;

    string role = Context.Request.HttpContext.Session.GetString("role");
    string user = Context.Request.HttpContext.Session.GetString("UserName");
    string name = Context.Request.HttpContext.Session.GetString("name");

    if (string.IsNullOrEmpty(role) || string.IsNullOrEmpty(user) || string.IsNullOrEmpty(name))
    {
        Context.Response.Redirect("/Home/Login");
        return;
    }
}

<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"]</title>
    <style>
        *{
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            font-family: Arial, sans-serif;
            font-size: 12px;
            line-height: 1.2;
            margin: 5px;
            padding: 0;
            width: 80mm; /* Standard receipt printer width */
            max-width: 80mm;
        }

        .header {
            text-align: center;
            border-bottom: 1px solid #000;
            padding-bottom: 5px;
            margin-bottom: 8px;
        }

        .company-name {
            font-size: 14px;
            font-weight: bold;
            color: #FC2947;
        }

        .gate-pass-title {
            font-size: 12px;
            font-weight: bold;
            margin: 5px 0;
        }

        .vehicle-info {
            display: flex;
            justify-content: space-between;
            margin-bottom: 8px;
            flex-wrap: wrap;
        }

        .vehicle-info div {
            flex: 1 1 45%;
            margin-bottom: 3px;
        }

        .driver-route-info {
            display: flex;
            justify-content: space-between;
            width: 100%;
        }

        .driver-route-info > div {
            flex: 1 1 45%;
            margin-bottom: 3px;
        }

        .details-table {
            width: 100%;
            border-collapse: collapse;
            font-size: 10px;
        }

            .details-table th,
            .details-table td {
                border: 1px solid #000;
                padding: 3px;
                text-align: left;
            }

            .details-table th {
                background-color: #f0f0f0;
                font-weight: bold;
            }

        .summary {
            margin-top: 8px;
            padding: 5px;
            border-top: 1px dashed #000;
            font-weight: bold;
        }

        .signature-row {
            display: flex;
            justify-content: space-between;
            margin-top: 8px;
        }

        .signature-box {
            flex: 1;
            text-align: center;
        }

            .signature-box:first-child {
                margin-right: 10px;
            }

        .print-button {
            margin: 10px 0;
            text-align: center;
        }

        .btn {
            padding: 5px 10px;
            border-radius: 3px;
            text-decoration: none;
            display: inline-block;
            margin: 0 3px;
            font-size: 12px;
        }

        .btn-primary {
            background-color: #007bff;
            color: white;
            border: 1px solid #007bff;
        }

        .btn-secondary {
            background-color: #6c757d;
            color: white;
            border: 1px solid #6c757d;
        }

        @@media print {
            .print-button

        {
            display: none;
        }

        body {
            margin: 0;
            padding: 2mm;
        }

        }

        /* Ensure no page breaks inside important elements */
        .header, .vehicle-info, .details-table, .summary {
            page-break-inside: avoid;
        }
        
        #barcode {
            width: 100%;
            height: 40px;
            margin: 0;
            padding: 0;
        }
    </style>
    <script src="https://cdn.jsdelivr.net/npm/jsbarcode@3.11.5/dist/JsBarcode.all.min.js"></script>
</head>
<body>
    <div class="print-button">
        <button class="btn btn-primary" onclick="window.print()">Print Gate Pass</button>
        <a href="@Url.Action("Index", "GatePass")" class="btn btn-secondary">Back to List</a>
    </div>

    <div class="header">
        <div class="company-name">Haldiram Foods International Pvt. Ltd.</div>
        <div class="gate-pass-title">VEHICLE GATE PASS</div>
        <svg id="barcode"></svg>
    </div>

    <div class="vehicle-info">
        <div><strong>Vehicle:</strong> @Model.TruckNumber</div>
        <div><strong>Date:</strong> @Model.DispatchDate.ToString("dd/MM/yy")</div>
        <div class="driver-route-info">
            <div><strong>Route:</strong> @Model.Route</div>
            <div><strong>Driver:</strong> _____________</div>
        </div>
    </div>

    <table class="details-table">
        <thead>
            <tr>
                <th>Customer</th>
                <th>Crates</th>
                <th>Cartons</th>
                <th>Numbers</th>
            </tr>
        </thead>
        <tbody>
            @if (Model.CustomerDetails != null && Model.CustomerDetails.Any())
            {
                @foreach (var customer in Model.CustomerDetails)
                {
                    <tr>
                        <td>@customer.CustomerName</td>
                        <td>@customer.Crates</td>
                        <td>@customer.Cartons</td>
                        <td>@customer.Numbers</td>
                    </tr>
                }
                <tr style="font-weight: bold;">
                    <td>Total: @Model.CustomerCount</td>
                    <td>@Model.CustomerDetails.Sum(c => c.Crates)</td>
                    <td>@Model.CustomerDetails.Sum(c => c.Cartons)</td>
                    <td>@Model.CustomerDetails.Sum(c => c.Numbers)</td>
                </tr>
            }
            else
            {
                <tr>
                    <td colspan="4">No customer details</td>
                </tr>
            }
        </tbody>
    </table>

    <div class="summary">
        <div class="signature-row">
            <div class="signature-box">
                Driver Signature<br>
                ___________________
            </div>
            <div class="signature-box">
                Security Signature<br>
                ___________________
            </div>
        </div>
    </div>
    <script>
        JsBarcode("#barcode", "@Model.TruckNumber#@Model.DispatchDate.ToString("dd/MM/yy")#@Model.Route", {
            format: "CODE128",
            lineColor: "#000",
            width: 2,
            height: 40,
            displayValue: true,
            fontSize: 12,
            margin: 0
        });
    </script>
</body>
</html>